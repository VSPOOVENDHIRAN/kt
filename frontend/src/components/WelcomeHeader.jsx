import React from 'react';

// This component is now removed - authentication handled in navbar
// Keeping file for compatibility but rendering nothing
const WelcomeHeader = () => {
    return null;
};

export default WelcomeHeader;
